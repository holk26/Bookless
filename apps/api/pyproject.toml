[project]
name = "bookless-api"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy>=2.0",
  "pydantic-settings",
  "alembic",
  "psycopg[binary]"
]

[tool.black]
line-length = 88
